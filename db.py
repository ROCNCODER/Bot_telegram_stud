import pymysql



try: 
  con = connect(
    host="",
    port="",
    user_name="",
    name_db=""
  )
except Ex es e:
    print(e)  

for i in crute

# Подключить словари 
with connection.cursor() as  cursor:
  inset_qver= "INSERT INTO `vacansian`(name, link, date) VALUES (ELEVENT[0], ELEMENT[1], ELEMENT[2])"
  cursor.execute(inset_qver)
  cursor.commit()
  
    
 
